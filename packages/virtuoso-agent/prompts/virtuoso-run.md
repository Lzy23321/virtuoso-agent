---
description: Validate and run a Virtuoso task file
argument-hint: "<task-file>"
---

Validate the Virtuoso task file `$1` with `virtuoso_task` using `action: "validate"`. If it is valid, call the same tool with `action: "run"`, then summarize metrics, proposal, and artifacts.
